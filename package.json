{
  "name": "enrollpro",
  "private": true,
  "scripts": {
    "dev:server": "pnpm --filter server dev",
    "dev:client": "pnpm --filter client dev",
    "dev": "pnpm --filter server --filter client --parallel dev"
  }
}
